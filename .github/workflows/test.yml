name: Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up CMake
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake

      - name: Install OpenCV (if needed)
        run: |
          # Install OpenCV dependencies and OpenCV itself if required
          # Example commands:
          sudo apt-get install -y libopencv-dev

      - name: Build C++ project
        run: |
          mkdir build
          cd build
          cmake ..
          make

      - name: Run tests
        run: |
          cd build
          # Assuming you have test cases using Google Test
          ctest --output-on-failure  # Runs all tests using CTest
